Database setup steps:

1. Enter schema.sql into your database UI (SequelPro or SQL Workbench) to create your database.
2. On the command line, and from the root of your project folder, run 'node server.js'.
3. Enter seeds.sql into your database UI (SequelPro or SQL Workbench) to insert the starter data.